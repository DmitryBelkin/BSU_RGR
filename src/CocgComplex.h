#include "MVFunctions.h"

void CocgComplex(
	  vector < int    > &ig
	, vector < int    > &jg
	, vector < double > &ggl
	, vector < double > &di
	, vector < int    > &ijg
	, vector < int    > &idi
	, vector < double > &rightPart
	, int                blockSize
	, vector < double > &result
	, double             epsilon
	, int                maxiter
	);
