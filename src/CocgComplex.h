#include "MVFunctions.h"

void CocgComplex(
	        vector < int    > &ig
	,       vector < int    > &jg
	,       vector < double > &gg
	,       vector < double > &di
	,       vector < int    > &ijg
	,       vector < int    > &idi
	,       vector < double > &rightPart
	, const int                blockSize
	,       vector < double > &result
	, const double             epsilon
	, const int                maxiter
	);
